import { describe, it, expect } from 'vitest'

describe('Concurrency Guards', () => {
  it.todo('should reject second submission when one is in pending_checks')
  it.todo('should reject second submission when one is in pending_review')
  it.todo('should allow new submission after prior is approved')
  it.todo('should allow new submission after prior is rejected')
})
